const DownVector = (props) => {
  const { className } = props;
  return (
    <div>
      <img
        className={`${className} w-[821px] h-[959px]`}
        alt="Group"
        src="images/group.png"
      />
    </div>
  );
};

export default DownVector;
